export enum RssGeneratorType {
    RSS = 'rss',
    WEBSITE = 'website'
}

export enum RssGeneratorFrequency {
    HOUR = 'hourly',
    DAY = 'daily',
}

export enum RssTaskStatus {
    pending = 'pending',
    finished = 'finished'
}